{
  "version": "2.0.0",
  "tasks": [
      {
          "label": "build debug",
          "type": "shell",
          "command": "mkdir -p ${workspaceFolder}/build && cd ${workspaceFolder}/build && cmake .. -DFETCHCONTENT_QUIET=OFF -DPYTHON_EXECUTABLE=$(python3 -c \"import sys; print(sys.executable)\") && cmake --build . --config Debug",
          "group": {
              "kind": "build",
              "isDefault": true
          }
      }
  ]
}
